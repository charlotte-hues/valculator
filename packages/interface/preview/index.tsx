import React from "react";
import ReactDOM from "react-dom/client";
import { Valculator } from "../src/main";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Valculator />
  </React.StrictMode>
);
